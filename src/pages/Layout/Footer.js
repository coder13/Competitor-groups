import React from 'react';

export default function Footer() {
  return (
    <footer className="p-2 text-xs">
      <span>
        All times are formatted to your locale timezone. Start times may not match the competition
        timezone's times.
      </span>
    </footer>
  );
}
