export default function Footer() {
  return (
    <footer className="p-2 text-xs w-full">
      <span>
        All times are formatted to your local timezone. Start times may not match the competition
        timezone's times.
      </span>
    </footer>
  );
}
